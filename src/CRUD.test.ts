export {};

xit("CRUD task", () => {
  xit("Can see the correct empty fields in create mode", () => {
    expect(1).toBe(2);
  });

  xit("Can see the correctly filled fields on edit mode", () => {
    expect(1).toBe(2);
  });

  xit("Can create a valid task", () => {
    expect(1).toBe(2);
  });

  xit("Can not create an invalid task with missing attributes", () => {
    expect(1).toBe(2);
  });

  xit("Can edit a task", () => {
    expect(1).toBe(2);
  });

  xit("Can not edit a task with missing attributes", () => {
    expect(1).toBe(2);
  });

  xit("Can delete a task", () => {
    expect(1).toBe(2);
  });

  xit("Can add an image to a task", () => {
    expect(1).toBe(2);
  });
});
