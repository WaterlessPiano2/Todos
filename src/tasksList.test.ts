export {};

//Prepare tests by adding some new tasks and deleting it fter
// So the tasks list is not empty
// Make one of the tests expires on now (or inn the past) so we can test expired tasks

xit("Tasks List", () => {
  xit("Can see a task's title and due date", () => {
    expect(1).toBe(2);
  });

  xit("Can see a test that is expired", () => {
    expect(1).toBe(2);
  });

  xit("Can see tests that are incomplete", () => {
    expect(1).toBe(2);
  });

  xit("Can mark a task complete", () => {
    expect(1).toBe(2);
  });

  xit("Can see more details about a task", () => {
    expect(1).toBe(2);
  });
});
