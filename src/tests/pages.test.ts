export {};

describe("pages", () => {
  xit("Show message when task list is empty", () => {
    expect(1).toBe(2);
  });

  xit("Can see the create task button", () => {
    expect(1).toBe(2);
  });

  xit("Can see the side menu with Tasks and About buttons", () => {
    expect(1).toBe(2);
  });

  xit("Can go to the About page using the side menu", () => {
    expect(1).toBe(2);
  });

  xit("Can go to the Tasks page using the side menu", () => {
    expect(1).toBe(2);
  });

  xit("On empty tasks list show message", () => {
    expect(1).toBe(2);
  });
});
